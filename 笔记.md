**1、新增英文
2、增加创作方式（精细化，让用户与AI深度协同创作的过程）**
3、新增多个声色可选择
4、字体优化和标点符合优化，改成楷体
**5、异常提示语，以及交互流程优化（敏感词/生成失败可先跳过或在后台加一步检测敏感词问题，优化后再生成）"**

丑小鸭（幼年）：一只灰褐色羽毛的小水禽，体型明显大于同窝雏鸭，喙宽而略钝，眼睛圆大呈深棕色，腿脚粗壮，走路时身体微微摇晃。羽毛蓬松无光泽，翅膀短小，整体轮廓笨拙。
农场动物们：一群家禽和家畜组成的群体：包括红冠白羽的母鸡、黄褐色虎斑家猫、棕白相间的短毛家犬。它们站立或蹲坐在农场地面，面部朝向画面中心，表情带有轻蔑或好奇。
场景：一个农家院落旁的浅水池塘，水面平静泛绿，岸边堆着金黄色干稻草，围有木质篱笆。背景可见低矮农舍屋顶和几棵果树。地面为压实的泥土，散落谷粒，周围有咯咯叫的鸡群活动痕迹。
画面：丑小鸭（幼年）在浅水中奋力划动双蹼，水花四溅打湿羽毛，身体歪斜几乎要摔倒。岸边，农场动物们——母鸡拍翅大笑、家猫捂嘴偷笑、家犬仰头狂吠——全都面向水面，表情夸张。
日式卡通风：具有日本漫画或动画的特点，线条流畅，色彩清新。

从大纲进入

只要存在 进行中：在后台继续、放弃生成
全部 已完成：去阅读
只有 生成失败 或 只有 生成失败 和 已完成：放弃生成

我不希望右上角有 继续生成，因为失败了，需要用户单个处理。
以上逻辑合理吗。

如果是从我的绘本点击 生成失败、已取消 进入。刚进入，界面肯定没有生成中，必定存在已失败，可能有已完成，此时 右上角不应该显示 放弃生成，而应该是“关闭”对吧。其它情况，和从大纲进入，一致了。

接下来的一个改动就比较大了。原本生成绘本阶段是显示js\generation-progress.js 弹窗，这个弹窗只显示进度，无法看到实时生成的图片。而且一旦绘本生成失败（只有存在任意一张失败，绘本状态就失败），那么已经生成的图片，也是看不到的，必须全部生成成功，才能阅读查看。而且，一旦由于敏感词问题，导致的生图失败，只能后端重试，如果重试还无法解决，用户也无能为力，因为生图的底层提示词，是不对用户开放的，流程上就没有用户修改某张生图提示词进行重试的功能。。为了解决这个问题，我们新设计了js\storybook-generation.js弹窗组件，替代之前的进度组件。接下来我描述一下这个弹窗组件。用户在 大纲确认 界面，点击 生成绘本，会弹出这个 生成绘本 界面，流程上分为 分析大纲、生成脚本、生成图片、完成。分析大纲、生成脚本 这两步，和之前进度组件逻辑一样，这个你可以查看之前的需求文档了解。进入生成绘本界面后，默认就处于分析大纲 这一步，下方显示固定的提示语（提示语含提示标题和提示明细），为什么提示语固定呢，因为分析大纲这一步，只是在等待脚本生那边的进度，一旦生成脚本那边通过流式输出检测到对应的标记时，就会切到 生成脚本那一步，然后根据检测到的标记，下方的提示词实时切换。提示语都还是和之前进度弹窗中的一致。所以这两步，只是呈现方式改了，内容还是和之前一样。但是到了生成图片这一步，就和之前完全不同了。会根据脚本内容，显示封面+全部页的卡片。这个卡片会显示是哪一页，旁白文字、图片。图片部分，若是生成中，会显示生成中状态，若是生成成功，会实时显示图片。若生成失败了，会显示失败提示，并提供操作按钮（这里分为两种，一种是敏感词导致的失败，提供修改提示词按钮，用户点击可以修改提示词，重新生成。一种是其它原因导致的失败，用户可以点击重新生成。）这里我还要说一点，我们的后端处理，一旦失败，会自动重试多次，多次都失败，才停止再次生成，如果是敏感词导致的失败，后端也会通过净化提示词尝试，都不行，才停止重试。我们这个界面中显示的失败，应是后端自动重试净化无效后，停止生图任务的情况。在描述一下修改提示词的弹窗，由于我们的生图提示词，是将脚本中的角色、场景、画面 三个内容+用户选择的风格的提示词内容 拼接而成，所以这边会将三个部分的内容拆开展示，让用户可以分别编辑。然后我们再描述一下右上角的操作按钮，不同情况下如何显示。
## 一、生成流程逻辑调整
### 1. 失败处理逻辑
- **旧逻辑**：某张图生成失败后，后续图片不再生成，绘本立即标记为"生成失败"
- **新逻辑**：某张图生成失败不影响其他图片生成，所有生图任务结束后再判定绘本状态
### 2. 取消处理逻辑
- 用户点击"取消生成"后，未进行的生图任务停止，绘本标记为"已取消"
- 已取消的绘本中可能存在：已完成的图片、失败的图片（未生成的也算失败处理）
### 3. 继续生成逻辑
- 生成失败/已取消的绘本支持继续生成
- 用户可在卡片上单独处理失败的图片（修改提示词/重新生成）
## 二、界面交互调整

### 1. 生成绘本窗口右上角按钮

| 状态                           | 右上角按钮          |
| ------------------------------ | ------------------- |
| 步骤1、2（分析大纲、生成脚本） | 后台生成 + 取消生成 |
| 步骤3有图片正在生成            | 后台生成 + 取消生成 |
| 全部完成                       | 去阅读              |
| 无生成中（失败/取消状态）      | 关闭                |

### 2. 左侧关闭按钮

- 已移除，统一通过右上角按钮操作

### 3. 取消生成二次确认

- 提示语："确定要取消生成吗？当前进度会保留，可随时继续。"
- 按钮："继续生成" / "停止生成"
- Toast提示："已取消生成"
